---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: ghost
  namespace: dev
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.chart-image: glob:dev-*
spec:
  releaseName: ghost
  chart:
    git: git@github.com:Rocklviv/flux-test-repo
    path: charts/ghost
    ref: dev
  values:
    image: rocklviv/ghost:dev-3
    replicaCount: 1
    hpa:
      enabled: false
